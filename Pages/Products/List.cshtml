@page
@model razor_pages.Pages.Products.ListModel

<h2>Danh sách sản phẩm</h2>
<ul>
    @foreach (var product in Model.Products)
    {
        <li>
            <a asp-page="Details" asp-route-id="@product.Id">@product.Name - @product.Price$</a>
        </li>
    }
</ul>